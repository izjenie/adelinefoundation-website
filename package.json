{
  "name": "adelinefoundation-website",
  "version": "1.0.0",
  "scripts": {
    "build": "cd react-app && npm install && npm run build && cp -r dist ../dist",
    "dev": "cd react-app && npm run dev"
  }
}
